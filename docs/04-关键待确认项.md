# 关键待确认项

已确认的范围不再重复询问。以下按真正影响实施的阶段提出，不阻塞当前骨架。

| 时点 | 未决事项 | 当前处理 |
|---|---|---|
| 2A 人口接入 | 参考目录新增 chn_pop_2025_CN_1km_R2025A_UA_v1.tif，是否为拟用政府数据，来源部门/时间/波段单位说明 | 只读检查元数据；不能根据文件名确认业务来源与单位，也不要求重新决定已确认的“人/km²”口径 |
| 2B 铁塔接入 | 首份样本字段、坐标系、共址资格依据 | 使用显式映射；未明确资格不自动判为可用 |
| 3–4 高度/约束 | 高度采用海拔或相对地面，垂直基准；净空裕度、方向间隔及硬约束数据来源 | 不沿用原型 30m 作为正式标准；无 DEM 标未校验 |
| 4 环境风险 | GRC 分级规则、工程风险评分参数、验收样例和工作网格分辨率 | 只保留环境/航路规划风险接口；模型版本和参数公开 |
| 5–6 覆盖 | 各必需分系统覆盖比例/重数要求，布站间距和共址搜索范围，允许的数值误差 | 用户规则配置；不照搬 0.9R/2km/1.2 为统一门槛 |
| 后续技术可靠性 | MTBF、`1e-6` 对应的事件定义、时间基准、相关失效与公式出处 | 独立技术风险接口；未确认前不判定，也不作为覆盖阻塞条件 |
| 后续生命/财产风险 | 各自模型、单位、阈值、数据源和验收样例 | 只建接口和多状态；确认完上述字段后再启用定量判定 |

实施自选：Streamlit、JSON 版本化清单、内部米制算法接口、S 表示监视、不默认在线底图。这些无需再次确认。PDF 引擎在导出阶段做 Windows 兼容验证后选择。
