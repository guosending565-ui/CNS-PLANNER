# 第一阶段验证记录

2026-09-08；Windows，Python 3.13.9，Streamlit 1.51.0，pytest 8.4.2。

- `python -m pytest -q`：9 passed。覆盖元信息往返、非法 JSON/字段/版本/边界拒绝、未注册算法拒绝、Streamlit 创建项目、应用边界、非法边界保留旧值及后四页无异常。
- `python -m streamlit run app.py --server.port 8501`：本机服务启动成功。
- `http://127.0.0.1:8501/_stcore/health`：返回 ok。
- `git diff --check`：无报错（当前文件为新建未提交）。

自动页面验证使用 Streamlit AppTest，不等同于浏览器视觉验收。下载/文件选择器实际交互及刷新恢复需用户按 README 验收；JSON 编解码已自动验证。未验证交互地图组件、GeoTIFF 读取、路由或覆盖算法，这些尚未实现。
